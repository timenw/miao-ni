android {
    compileSdkVersion 34
    buildToolsVersion "34.0.0"

    sourceSets {
        main.java.srcDirs = ['src/main/kotlin']
    }

    defaultConfig {
        minSdkVersion 26
        targetSdkVersion 34
    }
}